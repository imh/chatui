Eventually, a better UI for chatgpt (i want system prompts and latex rendering and gpt4)

To run:

```
cd backend
python app.py
```

```
cd ui
yarn start
```

Then go to `localhost:3000` and start typing.
